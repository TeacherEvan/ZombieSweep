// Empty stub for the optional `phaser3spectorjs` dev dependency that Phaser's
// WebGLRenderer requires at import time. This project uses the CANVAS renderer
// and never instantiates WebGL, so the spectre is unused. See vitest.config.ts.
export default {};
